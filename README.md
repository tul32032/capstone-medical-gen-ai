# capstone-medical-gen-ai
To run the main branch and test, you need env files and docker compose. 

The docker compose commands are these: (go into root folder)

To build the files:
docker compose up --build 

To delete:
docker compose down -v
